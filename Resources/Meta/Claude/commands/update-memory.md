Manually trigger session close memory updates.

Execute the **Session Close Process** in `.claude/rules/core/session-protocol.md`, exactly as written there (work-state.md always; memory.md, reminders.md, milestone frontmatter, and Session Log conditionally). Then show the user what changed.

Do not restate the close steps here — the protocol is the single source of truth. A duplicated copy of the steps in this file drifted out of sync with the protocol once already; a pointer can't drift.
